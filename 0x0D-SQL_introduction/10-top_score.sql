-- Lists all records of the table "second_table"
-- Results shld display both the score and the name(in this order)
-- Records shld be ordered by score (top first)
SELECT score, name FROM second_table ORDER BY score DESC;
